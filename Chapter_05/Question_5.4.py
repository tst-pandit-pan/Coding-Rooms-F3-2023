import random 		     # to import the built-in module / library

n = random.random()      # assign a random number between 0.0 and 1.0 to n
print("The value of n is", n , "." )


m = random.randint(1,10)	# m = random int between 1 and 10 inclusive
print("The value of m is", m , "." )
